{
	"name": "ignite-30-plans-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy-staging": "npx wrangler deploy --env staging",
		"deploy-production": "npx wrangler deploy --env production",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.6.16",
		"typescript": "^5.8.3",
		"vitest": "~3.2.4",
		"wrangler": "^4.54.0"
	},
	"dependencies": {
		"@neondatabase/serverless": "^1.0.0",
		"@sanity/client": "^7.9.0",
		"hono": "^4.7.8"
	}
}
